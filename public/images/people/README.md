# Place team member avatars here

Recommended naming convention: lowercased first name, e.g. `alice.jpg`.

Reference from `src/data/team.json`:

```json
{ "avatar": "/images/people/alice.jpg" }
```

If an avatar is missing or fails to load, the team card falls back to a
text placeholder showing the member's initials.
